
language: r
sudo: false
dist: trusty
cache: packages

env:
  matrix:
    - NOT_CRAN=true

after_failure:
  - ./pkg-build.sh dump_logs

notifications:
  email:
    on_success: change
    on_failure: change

after_success:
  - Rscript -e 'covr::codecov()'

