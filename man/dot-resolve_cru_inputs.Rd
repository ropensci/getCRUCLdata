% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal-resolve_cru_inputs.R
\name{.resolve_cru_inputs}
\alias{.resolve_cru_inputs}
\title{Resolve CRU variable flags and input file paths}
\usage{
.resolve_cru_inputs(pre = FALSE, tmp = FALSE, x = NULL, ...)
}
\arguments{
\item{pre}{Logical. Whether precipitation (\code{pre}) is requested.}

\item{tmp}{Logical. Whether temperature (\code{tmp}) is requested.}

\item{x}{Character. Either a directory containing CRU files or one or more
CRU \code{.dat.gz} file paths.}

\item{...}{Ignored. Allows future expansion.}
}
\value{
A list with two elements:
\describe{
\item{vars}{Named logical vector of CRU variable flags.}
\item{files}{Character vector of resolved CRU file paths.}
}
}
\description{
Internal helper used by higher-level CRU readers to convert user-facing
arguments (e.g., \code{pre = TRUE}, \code{tmp = TRUE}, \verb{x = <dir>}) into a standardised
structure containing:
}
\details{
\itemize{
\item a named logical vector of requested CRU variables (\code{vars})
\item a character vector of file paths (\code{files})
}

If \code{x} is a directory, all matching CRU files (\code{grid_10min_*.dat.gz}) inside
that directory are returned.
If \code{x} is one or more file paths, they are returned unchanged.

This function does \strong{not} read or validate file contents; it only resolves
which files should be processed downstream.
}
\keyword{internal}
